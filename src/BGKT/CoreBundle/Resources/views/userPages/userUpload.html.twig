<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="{{ asset("css/userPagesStyles/userUploadPage.css") }}" />
    <link rel="stylesheet" href="{{ asset("css/dropzone.css") }}" />

    <!---    <link rel="stylesheet" href="{##}{asset('/css/userPagesStyles/userHomePage.css')}}">
     Boostrap -->
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <!-- Optional theme -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css" integrity="sha384-rHyoN1iRsVXV4nD0JutlnGaslCJuC7uwjduW9SVrLvRYooPp2bWYgmgJQIXwl/Sp" crossorigin="anonymous">
    <!-- Latest compiled and minified JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>


    {#<script src="{{ asset("js/UploadCours.js") }}"></script>#}
    <script src="{{ asset("js/dropzone.js") }}"></script>

    <meta name="viewport" content="width=device-width, initial-scale=1">

</head>
<body>

<div class="container">
    <div class="intro"><i class="glyphicon glyphicon-education icon"></i>
        <h2 class="text-center">Vous êtes sur l'espace de dépôt des Cours</h2>

    </div>
    <div class="test">

        <div class="panel">
            <div class="panel-heading">
                <h4><strong>Télécharger mon cours</strong></h4>
            </div>
            <div class="panel-body">

                <!-- Standar Form -->
                <h4>Sélectionnez le cours à télécharger sur la plateforme</h4>

                <div class="dropzone my-dropzone" id="form_snippet_image" action="{{path('ajax_snippet_image_send')}}">
                </div>

                <h3>Liste des Cours</h3>
                {% for document in lstUploadedDocuments %}
                    <p>
                    <div class="col-md-8">
                        Identifiant : {{ document.id}}</br>
                        Nom : {{ document.name }}</br>
                        <a href="{{asset('uploads/documents/'~document.getName) }}">Voir le cours (PDF)</a>
                        {#<a href="{{ asset('uploads/documents' ~ document.name) }}">View brochure (PDF)</a>#}
                        {#<a href="{{  app.request.scheme ~ '://' ~ app.request.host ~ '/' ~ document.getWebPath  }}">View brochure (PDF)</a>#}


                    </div></br>
                    </p>
                {% endfor %}


                <!-- Upload Finished -->
                {#<div class="js-upload-finished">#}
                    {#<h3>Fichier(s) trait(é)s</h3>#}
                    {#<div class="list-group">#}
                        {#<a href="#" class="list-group-item list-group-item-success"><span class="badge alert-success pull-right">Téléchargé avec succès</span>{{ document.name }}</a>#}
                    {#</div>#}
                {#</div>#}
            {#</div>#}
        </div>

    </div>

</div>
</div>
</body>
</html>